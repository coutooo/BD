-- a)
-- π numUtente, nome (σ numPresc=null (π numUtente, nome (paciente) ⟕ prescricao))
-- b)
-- γ especialidade; count(numPresc)->Numero_Prescricoes (prescricao ⟕numMedico=numSNS medico)
-- c)
-- γ farmacia; count(numPresc) -> Numero_Prescricoes_Processadas (σ farmacia != null (prescricao))
-- d)
-- (π nome (σ numRegFarm=906 farmaco)) - (π nomeFarmaco (σ numRegFarm=906 presc_farmaco))
-- e)
-- σ farmacia ≠ null γ farmacia,numRegFarm; count(numRegFarm)-> Numero_Farmacos (presc_farmaco ⨝ prescricao)
-- f)
π numUtente, nome, numMedico (paciente ⨝ σ num > 1 (γ numUtente, numMedico; count(numUtente) -> num prescricao))